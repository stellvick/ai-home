- A aplicação deve usar typescript, react 19 ultima versão, vite, tailwind 4 e heroui. Os dados serão obtidos atravez de uma api ja pronta mas mock deve ser usado inicialmente. A função deve estar pronta para ser implementada. Não são necessarios testes automatizados.
- Usar componentes do HeroUI: https://www.heroui.com/docs/guide/introduction antes de fazer qualquer componente. Alem de usar os componentes the componente appearance should be modern and visually pleasing.

Atenção ao instalar o tailwind: [plugin:vite:css] [postcss] It looks like you're trying to use `tailwindcss` directly as a PostCSS plugin. The PostCSS plugin has moved to a separate package, so to continue using Tailwind CSS with PostCSS you'll need to install `@tailwindcss/postcss` and update your PostCSS configuration.


Usar react query, yup, zustand, encrypt-storage, react use, lucide react.